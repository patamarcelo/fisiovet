import React from 'react';
import { Text } from 'react-native';
import Screen from '../_ui/Screen';

export default function AgendaMap() {
    return (
        <Screen>
            <Text style={{ fontSize: 20, fontWeight: '600' }}>Agenda no Mapa</Text>
            <Text>Pinos por consulta e navegação</Text>
        </Screen>
    );
}